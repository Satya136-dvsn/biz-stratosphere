# ══════════════════════════════════════════════════════════════
# Biz Stratosphere – Secrets (TEMPLATE – fill before applying)
# ══════════════════════════════════════════════════════════════
apiVersion: v1
kind: Secret
metadata:
  name: biz-secrets
  namespace: biz-stratosphere
  labels:
    app.kubernetes.io/part-of: biz-stratosphere
type: Opaque
stringData:
  # Replace with real values before deploying
  DATABASE_URL: "postgresql://bizuser:changeme@postgres:5432/bizstratosphere"
  SUPABASE_URL: "https://your-project.supabase.co"
  SUPABASE_ANON_KEY: "your-anon-key"
  POSTGRES_USER: "bizuser"
  POSTGRES_PASSWORD: "changeme"
  POSTGRES_DB: "bizstratosphere"
