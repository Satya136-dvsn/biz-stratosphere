# This workflow is currently disabled
# Re-enable when backend deployment infrastructure is ready

# name: Deploy to Production

on:
    push:
        branches: [main]
    workflow_dispatch:

jobs:
    deploy:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v3

            - name: Setup Node.js
              uses: actions/setup-node@v3
              with:
                  node-version: "18"
                  cache: "npm"

            - name: Install dependencies
              run: npm ci

            - name: Build
              run: npm run build
              env:
                  VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
                  VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
                  VITE_GEMINI_API_KEY: ${{ secrets.VITE_GEMINI_API_KEY }}

            # Deployment step - configure based on your hosting
            # Vercel example (uncomment and add secrets if using):
            # - name: Deploy to Vercel
            #   uses: amondnet/vercel-action@v20
            #   with:
            #     vercel-token: ${{ secrets.VERCEL_TOKEN }}
            #     vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
            #     vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
            #     vercel-args: '--prod'

            # For now, just verify build succeeds
            - name: Build verification
              run: echo "Build completed successfully!"
