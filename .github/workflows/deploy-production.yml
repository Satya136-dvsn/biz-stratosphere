# This workflow is currently disabled
# Re-enable when backend deployment infrastructure is ready

# name: Deploy to Production

# on:
#   push:
#     branches: [ main ]
#   workflow_dispatch:

# Frontend is deployed via Netlify/Vercel
# Backend deployment requires infrastructure setup
